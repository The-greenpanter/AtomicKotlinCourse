// DataTypes/Task3.kt
package dataTypesExercise3

fun main() {
// 'a' + 1
val c1 = 'a' + 1

// 'a' + 25
val c2 = 'a' + 25

// 'E' - 2
val c3 = 'E' - 2

println(c1 + " "+ c2 + " "+c3+ " ") // b z C
//Characters are stored as numbers corresponding to their Unicode values,
// so adding an integer to a character produces a new character corresponding
// to the new code value.


}
